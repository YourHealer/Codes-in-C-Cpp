#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define n 20

function_string(char array[],int num)
{
	int i; 
	for(i=num;i<0;i--)
	{
		                                     /*需要用指针才能实现对内容的更改*/
	}
}

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() 
{
	int length;
	char string[n];
	
	puts(string);
	length=strlen(string);
	
	puts(string); 
	function_string(string,length);
	puts(string);
	
	return 0;
}
